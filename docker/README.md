# README

This directory contains files useful for building or using Docker images.

```
Docker
├── README.md
├── examples.sh
└── render
```

[`examples.sh``](examples.sh) provides bash functions for building and running the docker image
provided.

[`render``](render) is a script that can be used to render a quarto project. 